# Block 6 — Redis Caching

> 🔒 This file will be filled after Block 6 is reviewed.

## Topics That Will Be Covered

- What is Redis and why it's faster than a DB
- Cache-Aside pattern (read-through + write-invalidate)
- How to serialize/deserialize objects for Redis (JSON)
- Setting TTL (Time To Live) on cache entries
- Cache key design — what makes a good cache key
- When to cache vs when NOT to cache
- Cache invalidation — the hardest problem in CS
- `IDistributedCache` vs `StackExchange.Redis` directly
- Running Redis locally with Docker
